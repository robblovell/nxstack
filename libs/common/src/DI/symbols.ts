export const Symbols = {
    config: Symbol.for('config'),
    storage: Symbol.for('storage'),
    logger: Symbol.for('logger'),
    feathers : Symbol.for('feathers'),
    feature : Symbol.for('feature'),
    prometheusMetrics : Symbol.for('prometheusMetrics')
}
